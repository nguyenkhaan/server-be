# Setup build.ts file 
# Setup deoloy.yaml -> Github services to deploy a Docker to Docker Hub 
# Setup Docker file -> The Step to build the app (Coy from source) 
# Setup docker-compose.yml: Describe about your app to make it become an image in container 